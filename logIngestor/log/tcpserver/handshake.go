package tcpserver

func HandShake(peers TcpPeer) error {

	// This handshake func will check if the log type is same
	// for the client side
	// e.g. : Server side log :{{level: 1, message: "Something"}}
	// Then client should also same log format for the filters
	return nil
}
